# Skills Index

<!-- Skills are loaded on-demand when task keywords match their triggers. -->

| Skill | Triggers | Description |
| ----- | -------- | ----------- |
| ofd-harness | when executing an approved feature plan autonomously via an orchestrator subagent in an isolated worktree; keywords "OFD", "orchestrated delivery", "run the plan with subagents", "spawn orchestrator" | Run an approved feature plan autonomously via an orchestrator subagent in a worktree. |
<!-- Add skills as they are created. Each skill is a markdown file in this directory with YAML trigger frontmatter. -->
