# Agent Preferences

<!-- Add preferences as stated: code style, verbosity, workflow -->

- **[workflow/pr]** PRs that are not 100% final MUST be created as Draft (`gh pr create --draft`) — non-negotiable default. Mark ready (`gh pr ready`) only once final. Moreover: never create a PR before its self-review is complete. (2026-06-12)
