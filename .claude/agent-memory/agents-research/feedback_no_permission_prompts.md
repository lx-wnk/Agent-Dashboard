---
name: No permission prompts for tool usage
description: Never ask for permission before using tools like Obsidian search, web fetch, bash — just execute directly
type: feedback
---

Never ask the user for permission before using tools (Obsidian API calls, WebSearch, WebFetch, Bash, Read, etc.). Just execute directly without announcing or asking.

**Why:** User explicitly said they don't want to be asked — just do it.

**How to apply:** On every task, run tool calls (Obsidian search, file reads, web fetches, etc.) immediately without prefacing with "may I" or "is it okay if I". Start working, don't ask.
